# LinkedList Operation

Common Structure

```
class ListNode:
    self._init_(self, val):
        self.val = val
        self.next = None
```

Useful Technique

* Use a dummy node at the start to keep track of the head
* Check the last node.next is set to None after final operation
* Loop check or fast & slow pointers are covered in other section
